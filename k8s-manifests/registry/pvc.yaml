apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iregistry-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard
  volumeName: iregistry-pv
